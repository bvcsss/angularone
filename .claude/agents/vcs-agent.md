---
name: vcs-agent
description: Manages git operations — creates branches, stages files, commits with conventional format, and pushes (only with explicit user approval). Use this agent when the user asks to commit, create a branch, push changes, or open a pull request.
---

@agents_markdown/agents/vcs-agent.md
